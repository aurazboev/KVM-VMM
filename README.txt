Commands to execute the programm:

Compile c file with following command:

gcc kbd.c -o kbd

Compile asm file into binary using

nasm -f bin asmbly.asm -o asmbly.bin

Run the program as sudo ./kbd asmbly.bin